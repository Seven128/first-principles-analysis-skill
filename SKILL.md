---
name: first-principles-analysis
description: 使用第一性原理分析明确或隐含的问题，生成“最终目的—现状与要解决的问题—采用的方案/核心原理与实现机制”因果严密的中文文章；也用于迭代本 skill、定位提示词失效原因并用既有样例做回归。适用于技术原理、方案设计、商业、个人与群体问题；不用于单纯改写、无问题定义的资料罗列或把现成答案反向合理化。
---

# 第一性原理分析

本 Skill 有两种工作模式：

1. **文章生成模式**：根据用户输入直接生成第一性原理分析文章。
2. **Skill 迭代模式**：修改分析逻辑、提示词或工作流，并使用回归样例验证是否破坏既有能力。

根据用户请求自动选择模式；用户没有要求修改本 Skill 时，默认使用文章生成模式。

## 权威与材料顺序

处理任何任务时，先区分“任务事实”和“分析方法”：

- 用户明确给出的目标、范围、事实材料与输出要求构成本次任务契约。
- [`references/第一性原理分析逻辑.md`](references/第一性原理分析逻辑.md) 是分析方法的最高权威。
- [`references/第一性原理分析提示词.md`](references/第一性原理分析提示词.md) 是对分析逻辑的可执行展开，不能覆盖或打乱前者。
- [`references/回归样例/`](references/回归样例/) 只用于学习行文结构、定位退化和做回归，不自动成为当前命题的事实来源。
- [`references/经验/`](references/经验/) 中的结论只是候选论据；使用前必须重新检查适用主体、范围、前提与失效边界。

若分析提示词、样例或经验与分析逻辑冲突，以分析逻辑为准；若材料不足以支持事实结论，明确指出缺口，不静默补齐。

## 文章生成模式

### 1. 读取规则

开始前完整读取：

1. `references/第一性原理分析逻辑.md`
2. `references/第一性原理分析提示词.md`
3. 用户提供的任务材料

不要默认读取全部回归样例。只有在需要校准某类问题的结构、措辞或已知退化点时，才读取最相关的样例；样例中的事实不能直接迁移到当前问题。

### 2. 先确定真正的问题

在内部判断：

- 用户读完后要理解、判断、选择或完成什么；
- 用户已经知道什么，真正不知道什么；
- 这是**目标求解型**、**原理还原型**还是两者分段组成的混合问题；
- 哪个具体问题最能达成用户的真实目的。

不要把“介绍 X”“写一篇文章”“总结资料”直接当成分析问题。

### 3. 选择正确时间基线

- **目标求解型**：以当下为基线，分析主体怎样从当前状态达到目标状态。
- **原理还原型**：以 X 尚未形成或核心机制尚未加入时为形成前基线，分析为什么需要 X，以及为什么逐步形成当前形态。
- 只有问题明确涉及版本升级、替代或演进时，才专门讨论版本差异。

### 4. 构造内部因果链

目标求解型使用：

```text
固定主体与最终目的 O
→ 固定当下基线 A0
→ O 所需的顶层条件
→ A0 与 O 之间的顶层缺口和根问题
→ 设计原则
→ 方案与机制
→ 结果、成本与验证
```

原理还原型分成两层：

```text
问题层：
固定主体 + 形成前基线 A0 + 最终目的 O
→ 顶层能力缺口与固有限制
→ 根问题
```

```text
方案层：
A0
→ 当前首先需要恢复的条件
→ 设计原则 D1
→ 机制 M1
→ 新基线 A1
→ 派生问题 G2
→ ……
→ 最小成立形态
→ 当前阶段的性能、成本、安全与场景化优化
```

“现状与要解决的问题”只比较固定的 A0 与 O；方案加入后的新基线和派生问题只能在方案与机制部分展开。每一项机制都必须先由事实、任务要求和缺口正向推出。禁止先列出现有模块清单，再为既有答案补写理由。

### 5. 输出面向读者的文章

默认结构为：

1. **最终目的**
2. **现状与要解决的问题**
3. **采用的方案**，或原理还原型中的 **核心原理与实现机制**

结构服从真实因果关系，不固定问题数量、机制数量或标题层级。

必须遵守：

- “最终目的”原则上只写一句，只锁定分析终点；模型限制、技术取舍、性能成本和方案条件进入后文。
- “现状与要解决的问题”始终使用同一主体、同一 A0 与同一 O，只写最少充分事实、顶层能力缺口、固有限制、根问题及后果。
- 问题抽象层级与最终目的保持一致；不得用具体任务步骤、未来运行流程、接口、循环、队列、模块、数据结构或算法替代顶层问题。
- 原理还原型在方案部分先证明条件，再引入机制；前一机制加入后的派生问题在新基线上继续推导。
- 方案与机制按前文问题顺序逐项承接；每个问题有方案，每个机制有问题来源。
- 完整模块图、运行总览和连续案例只能在逐项论证之后出现。
- 外部环境只能作为影响主体状态转变的要求或条件，不能无说明地取代分析主体。
- 内部校准过程、材料分级、对象层级树、审计清单和提示词思考过程不进入正文，除非它们直接影响读者理解或用户明确要求。
- 事实、合理推断、待验证假设与价值取舍分开表达。
- 语言客观、白话、简练、直白、准确；不讨好用户，不制造黑话，不滥用“物理、绝对、完美、降维、跃迁”等修辞。

### 6. 输出前审计

至少检查：

- 分析的是明确问题，不是宽泛主题。
- 最终目的原则上只有一句，时间基线正确。
- 现状部分是否始终比较同一主体、A0 与 O。
- 问题层级是否与最终目的匹配，且只使用证明顶层缺口所需的最少事实。
- 现状部分是否泄漏了具体任务的动态过程、未来方案或实现细节。
- 方案加入后的 A1、A2 与派生问题是否只在方案部分出现。
- 每项机制都由当前基线中的真实缺口逐项正向推出。
- 没有在证明前列出完整答案。
- 没有无条件加入版本比较。
- 没有把内部推演脚手架写给读者。
- 任一机制都能反向追溯到根问题、缺口、基线与最终目的。

审计失败时先修正，再输出最终文章；不要输出草稿式思考过程。

## Skill 迭代模式

当用户要求修改、优化、审查或回归本 Skill 时：

1. 完整读取分析逻辑、分析提示词、`evals/regression-cases.json` 和相关回归样例。
2. 先定位失效属于哪一层：问题选择、时间基线、固定比较端点、主体/层级、事实边界、问题与方案分界、正向因果链、读者价值、文章结构或措辞。
3. 修改能够消除根因的最上游且最小的权威层；避免把同一规则重复写进多个位置。
4. 不因单个案例硬编码专用答案。新增规则应说明它解决的通用失效模式和适用边界。
5. 原始样例是回归基线。除非用户明确要求修订样例，禁止静默改写；如确需修改，记录原因并同步更新 `references/source-manifest.json`。
6. 对变更前后使用同一组代表性输入，按照 `evals/README.md` 检查结构性不变量，而不是要求逐字复现样例。
7. 运行：

```bash
python3 scripts/validate_skill.py
```

8. 交付时说明：根因、修改位置、覆盖的回归案例、仍存在的不确定性。

## 禁止事项

- 不把现有产品、模块或行业惯例当作不可质疑的前提。
- 不把具体实现误写成唯一必要条件。
- 不在“现状与要解决的问题”中预先完成方案设计或展开方案加入后的新基线。
- 不用具体任务的完整动态过程取代固定基线与最终目的之间的顶层缺口。
- 不为了结构整齐制造固定数量的问题或模块。
- 不把某个子类型、版本或单一产品的特征泛化为整个类别。
- 不把样例中的事实、结论或措辞无条件复制到新命题。
- 不以展示分析方法为正文目的。
