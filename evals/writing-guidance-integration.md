# 写作参考的采用记录

## 来源核验

2026-09-08 读取 OpenAI 官方 Model guidance 的 Using GPT-6 Astra 部分，具体位置是 [Personality and writing style](https://developers.openai.com/api/docs/guides/latest-model#personality-and-writing-style)。这是模型指南中的一节。该节有以“Avoid using slop words”开头的示例提示词，页面本身并不以这句话开头。读取的页面没有提供足以核实该节于 9 月 5 日首次上线的记录，本项目不把这个日期写成事实。

官方建议的相关方向包括：尽早说出重点，用朴素动词解释关系，减少套话、自造标签和未被问及的对立表达，按阅读需要选择格式。本记录概述相关建议，不复制完整词表或整段提示词。

## 本项目的适配

以下是针对本项目已有规则和用户反馈的取舍，不代表官方逐条原文：

- 输出规则第 5 节继续负责判断结论是否说全；第 6 节处理无信息的包装。删掉套话后仍只说半句话，依然不合格。
- 《写作规则卡》第 0、1、5 节补齐截图中的陈述句问题；第 3 节示范把抽象包装还原成已有材料支持的动作。
- 官方示例中的词汇和句式没有被复制为机械禁用表。技术术语、引语、代码标识符、真实比较、必要否定和不确定性仍须保真。
- 去掉无用的方法预告与重复收尾，不删除读者可见问题、目的型文章主链、用户要求的问答或影响结论的范围说明。
- 模型名称仅用于标明参考来源；规则按任务适用，不依赖特定模型，也不加入固定人格、口头禅或额外运行流程。

## 评测入口

`evals/local-conclusion-regression-cases.json` 保留原有 24 个案例，增加 8 个 `style-` 案例。生成输入仍由 `scripts/validate_writing.py` 的 `local_conclusion_input` 提供，只包含给定事实和请求；评审字段留在评审阶段。`tests/test_local_conclusions.py` 检查覆盖、输入隔离、复制和异常处理，不按词频或句长判定文章质量。

## 本轮输出自查

以下输出由本次修改规则的同一助手根据案例材料生成并自查，已经看过评审要求。它们用于核对规则能否表达所需结果，不是隔离上下文实验、独立模型回归或新旧规则盲测。没有由此计算通过率或提升幅度。

### 短要点

| 案例 | 本轮输出 | 自查要点 |
| --- | --- | --- |
| style-drop-fillers | 工具执行模型提出的操作，并把结果返回模型，供模型判断下一步。 | 主体、动作和用途完整；未夸大为任务成功。 |
| style-unpack-label | 工具执行操作后把结果返回模型，模型据此决定下一步操作。 | 拼装标签已还原为给定动作；未补入新机制。 |
| style-direct-status | 配置文件中的超时时间已修改并保存；测试尚未运行，修改是否生效仍未验证。 | 完成与未验证状态都保留，没有虚构其他修改范围。 |
| style-keep-real-comparison | 两个方案质量相同：A 三天完成，费用较低；B 两天完成，费用较高。应先确定截止时间，再从能按时完成的方案中选费用较低的。 | 时间、费用、质量和既定选择标准均保留。 |
| style-keep-terms-and-quote | 本例采用闭环控制，根据测得的输出调整下一轮输入。配置字段名为 `retry-after`。原句为“这一机制赋能后续判断”。 | 正式术语、字段和引语按要求保留，未把引语当作额外效果的证据。 |
| style-no-invented-action | 材料没有说明该模块执行什么操作，暂时无法准确描述其作用。 | 直接说出具体缺失，没有用想象的机制填空。 |
| style-preserve-negative-boundary | 本项目未经审核的申请不得付款；审核通过后仍需检查预算。 | 只删包装，必要条件和项目范围未改变。 |

### style-no-ceremony-article

以下为按给定备份材料生成的完整短文；保留结构问题和因果说明，正文不使用粗体。

---

# 文件备份为何需要保留历史版本并验证恢复

## 要分析的问题

为什么一份工作文件之外，还需要独立备份、历史版本和恢复演练？

## 最终目的

文件被误删或错误覆盖后，仍能恢复所需内容。

## 当前状态与要解决的问题

目前只有一份工作文件，误删后没有其他副本可供恢复。即使增加备份，持续覆盖旧副本也可能把最后一份正确内容替换掉。备份是否能正常恢复，目前还没有得到验证。

## 采用的方案

独立备份在工作文件被误删后提供恢复副本。它补足了当前没有其他副本的问题。

历史版本保留覆盖前的内容，减少错误同步到备份后无可用旧内容的风险。只保留最新副本，仍可能同时失去工作文件和备份中的正确内容。

恢复演练检查备份能否还原所需文件。保存了副本，并不能单独说明恢复过程已经可用。

这些措施针对误删、错误覆盖和恢复未经验证的问题，不能据此保证应对所有故障。

---

自查：各段直接说出具体作用；必要因果、恢复尚未验证以及故障范围均保留；没有用反复设问、口号或同义总结填充篇幅。完整文中的结构标题继续承担导航职责。

## 验证边界

最终自动检查状态以 PR 对应提交的 GitHub Actions 记录为准。以上样例自查与程序测试不证明真实生成已稳定改善。进一步评估应固定相同材料与模型配置，在隔离上下文中比较旧版和新版，再独立检查结论完整性、信息选择、必要因果和限定是否保真。
