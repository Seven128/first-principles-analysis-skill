# Agent 文章的失败片段

## 来源与范围

以下摘录来自 2026-09-10 用户在当前任务中提供的《Agent 是什么：第一性原理推演》。这里只保存与反馈有关的标题和片段，不是全文副本。用户说明文章由另一对话使用本 Skill 生成，但没有提供那次运行的提交号、模型设置或完整调用记录，因此不能把它视为某个已固定旧版本的受控输出。

这些片段用于定位回归，不进入生成输入。`cases.json` 中的材料为本次整理的受控材料，评审条件另存；它们也不代表已经执行的新旧模型实验。

## 问题选择与层级

原文同级标题：

> 2.1 大模型能够提出判断，但模型输出本身不会完成外部操作

> 2.2 普通程序已经能够执行任务，但预设流程不能覆盖所有任务路径

> 2.3 判断与执行之间的衔接，仍然需要人持续承担

原文 2.2 中已经有更接近主问题的判断：

> 下一步做什么往往取决于刚刚获得的信息，难以提前完整列出。

用户反馈要求把“开放任务需要多轮判断与执行”作为主要问题，把人工衔接放在这一问题下；幻觉、上下文和成本作为不可遗漏的限制。通用修复应检查主要缺口及父子关系，不把这些名称或三点数量写成所有问题的模板。

## 标题承载了过多职责说明

原文标题：

> 3.1 应用接管模型判断与实际执行之间的衔接

> 3.5 状态保存维持任务连续性，上下文管理决定模型当前能看到什么

> 3.6 完成判断必须对应任务要求，停止执行不一定意味着成功

用户希望直接使用能定位方案的短名称或具体动作。由本次讨论形成的修复方向是：名称标题与结论句分别检查；正文继续解释必要因果。工具执行和执行隔离仍需区分，持久化保存和本轮输入选择也应分别保留。

## 这份材料不能证明的内容

它能支持存在一次用户指出的实际阅读失败，不能量化失败率，不能证明某一条规则是唯一原因，也不能充当新版模型输出。根因判断仍需对照规则、其他迁移案例和后续独立输出检查。
