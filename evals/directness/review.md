# 本次修改与验证记录

## 修改依据

修改前提交：`753abadcdd30ade6ab79bceea383aafb8c9e8323`。用户在 2026-09-10 提供了 Agent 文章及两类反馈：现状部分的主要问题和父子关系不清；方案标题与正文使用过多职责描述、转折和限定。原始失败片段与来源边界见 `failure-excerpts.md`。

原运行规则要求结论用完整陈述，并提供了接近失败文章的长标题示例。这里将结论句和名称标题分开，同时修复问题归纳。规则与示例的一致性只能支持这次修复方向，不能证明它们是唯一原因或已经稳定改善实际生成。

## 已实现

- 主要缺口先由目标与事实推出，原因、表现、人工应对和后果放在对应问题下；独立限制保留。多轮任务不自动等于跨会话长任务。
- 问题与判断直接陈述，方案标题允许准确名称与具体动作，紧邻正文解释作用和因果；标题不再承担全部责任边界。
- 删除重复主体、转折铺垫、无效限定和职责重复。保留必要否定、证据限制及工具/沙箱、持久化/上下文等真实区别。
- 同步运行规则、正反例和评分要求。历史来源与原有自查记录保留。
- 新增 12 个受控案例，覆盖原失败、非技术迁移、并行条件、短标题、必要否定、未知、成本和完整因果。
- 新增独立对照准备、输出盲化和结果记录校验工具；日常 Skill 使用不要求这些工具或额外模型调用。

## 实际执行的验证

本地执行 `python3 -m unittest discover -s tests -p 'test_*.py' -v`，本次新增的 17 项单元测试全部通过；脚本通过 Python 编译检查。该本地工作区只包含新增比较脚本、测试及案例，不能据此声称旧仓库全部测试已在本地运行。

单元测试使用合成数据，覆盖生成字段分离、版本固定、输入输出哈希、盲化、来源声明、上下文声明、缺失输出、改动检测及评审引文校验。它们测试工具协议，不评价语言模型生成质量。

完整仓库校验交给本 PR 的 GitHub Actions，实际状态和日志以该 PR 对应提交的检查为准。没有成功记录前，不将其记为通过。

## 未执行

本轮没有实际调用隔离的生成模型或独立评审上下文，因此真实新旧版本生成、独立盲评、真实读者理解测试、阅读时间和稳定提升幅度均未执行。12 个案例及工具已经准备好，不等于这部分实验完成。

脚本的独立条件来自操作者声明，不能证明供应商实际隔离上下文；同一对话换角色属于自查。版本与材料固定、生成和评审分开、原始输出保留后，才能按 README 执行实际对照并追加结果。
